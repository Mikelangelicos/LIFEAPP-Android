<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:hasCode="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="LIFEAPP"
        android:supportsRtl="true">
        <activity android:name=".LanguageSettingsActivity" android:exported="false" android:label="@string/language" />
        <activity android:name=".MusicHubActivity" android:exported="false" android:label="@string/music" />
        <activity
            android:name=".LifeAppActivity"
            android:exported="true"
            android:icon="@mipmap/ic_launcher"
            android:label="LIFEAPP">
            <meta-data android:name="android.app.lib_name" android:value="lifeapp" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
